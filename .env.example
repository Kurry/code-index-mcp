# Code Index MCP Environment Variables
# Copy this file to .env and fill in your API keys

# Project directory to index
PROJECT_DIRECTORY=/path/to/your/project

# OpenAI API Key (required if using OpenAI embeddings)
OPEN_AI_KEY=your_openai_api_key_here

# Voyage API Key (required if using Voyage embeddings)
VOYAGE_API_KEY=your_voyage_api_key_here

# Qdrant Configuration 
# (required if using --use-qdrant-cloud flag)
QDRANT_URL=https://your-cluster-url.qdrant.tech
QDRANT_API_KEY=your_qdrant_api_key_here

# Optional configuration
# EMBEDDING_DIMENSION=1536  # Override default embedding dimensions
# CHUNK_SIZE=1000           # Size of code chunks for large files (if enabled)
# CACHE_LIMIT=1000          # Maximum number of embeddings to cache in memory
